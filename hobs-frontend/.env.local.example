# Base URL of the HoBS / ShopprHQ backend (no trailing slash).
# All API calls in lib/api.ts are built as `${NEXT_PUBLIC_API_BASE_URL}/api/v1/...`
# Local backend default is usually http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
